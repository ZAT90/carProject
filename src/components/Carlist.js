import React, { Component } from 'react';
import { View, Text } from 'react-native';
import {
  Card, CardSection, Input, Button, Spinner,
} from './common';

class Carlist extends Component {
  render() {
    return (
      <View>
        <Text>This is my car list</Text>
      </View>
    );
  }
}
export default Carlist;
